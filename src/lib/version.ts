// Bumped on every deploy by the assistant. Surfaced in the admin
// sidebar so you can refresh the page and confirm you're on the
// latest build (vs. an old cached tab).
export const APP_VERSION = "0.7.0";
